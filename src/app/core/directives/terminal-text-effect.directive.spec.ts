import { TerminalTextEffectDirective } from './terminal-text-effect.directive';

describe('TerminalTextEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new TerminalTextEffectDirective();
    expect(directive).toBeTruthy();
  });
});
